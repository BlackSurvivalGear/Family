/**
 * Firebase Configuration Template - config.example.js
 *
 * INSTRUCTIONS:
 * 1. Duplicate this file in the same directory and rename it to `config.js`.
 * 2. Replace the placeholder strings below with your own actual Firebase project configuration credentials
 *    obtained from the Firebase Console (Project Settings > General > Your apps > Web apps).
 * 3. Never commit your actual `config.js` file with real credentials to public repository control.
 */

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_AUTH_DOMAIN_HERE",
  projectId: "YOUR_PROJECT_ID_HERE",
  storageBucket: "YOUR_STORAGE_BUCKET_HERE",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID_HERE",
  appId: "YOUR_APP_ID_HERE"
};
